name: Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Release version (e.g., 2.2.19.bravura-6)'
        required: true
        default: '2.2.19.bravura-6'
        type: string
      release_branch:
        description: 'Release branch name'
        required: true
        default: 'release/2.2.19.bravura-6'
        type: string
      upload_to_github_packages:
        description: 'Upload Maven artifacts to GitHub Packages'
        required: false
        default: false
        type: boolean

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          java-version: '8'
          distribution: 'temurin'

      - name: Configure Git
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

      - name: Create or checkout release branch
        run: |
          BRANCH_NAME="${{ github.event.inputs.release_branch }}"
          # Check if branch exists locally or remotely
          if git show-ref --verify --quiet refs/heads/$BRANCH_NAME || git ls-remote --heads origin $BRANCH_NAME | grep -q $BRANCH_NAME; then
            echo "Branch $BRANCH_NAME already exists, checking out..."
            git fetch origin $BRANCH_NAME:$BRANCH_NAME 2>/dev/null || true
            git checkout $BRANCH_NAME
            git pull origin $BRANCH_NAME || true
          else
            echo "Creating new branch $BRANCH_NAME..."
            git checkout -b $BRANCH_NAME
          fi

      - name: Set Maven version
        run: |
          mvn versions:set -DnewVersion=${{ github.event.inputs.version }} -DgenerateBackupPoms=false

      - name: Verify version change
        run: |
          VERSION=$(mvn help:evaluate -Dexpression=project.version -q -DforceStdout)
          echo "Current version: $VERSION"
          if [ "$VERSION" != "${{ github.event.inputs.version }}" ]; then
            echo "Error: Version mismatch. Expected ${{ github.event.inputs.version }}, got $VERSION"
            exit 1
          fi

      - name: Build project
        run: mvn clean install -DskipTests

      - name: Commit version change
        run: |
          git add -A
          git commit -m "Set version to ${{ github.event.inputs.version }}" || echo "No changes to commit"

      - name: Push release branch
        run: |
          BRANCH_NAME="${{ github.event.inputs.release_branch }}"
          git push -u origin $BRANCH_NAME || git push origin $BRANCH_NAME || echo "Branch already up to date"

      - name: Create and push tag
        run: |
          TAG_NAME="${{ github.event.inputs.version }}"
          if git rev-parse "$TAG_NAME" >/dev/null 2>&1; then
            echo "Tag $TAG_NAME already exists, skipping..."
          else
            git tag -a "$TAG_NAME" -m "Release ${{ github.event.inputs.version }}"
            git push origin "$TAG_NAME"
          fi

      - name: Upload to GitHub Packages
        if: github.event.inputs.upload_to_github_packages == 'true'
        run: |
          # Configure Maven settings for GitHub Packages
          mkdir -p ~/.m2
          cat > ~/.m2/settings.xml <<EOF
          <settings xmlns="http://maven.apache.org/SETTINGS/1.0.0"
            xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
            xsi:schemaLocation="http://maven.apache.org/SETTINGS/1.0.0
            http://maven.apache.org/xsd/settings-1.0.0.xsd">
            <servers>
              <server>
                <id>github</id>
                <username>\${{ github.actor }}</username>
                <password>\${{ secrets.GITHUB_TOKEN }}</password>
              </server>
            </servers>
          </settings>
          EOF
          
          # Deploy to GitHub Packages using altDeploymentRepository
          REPO_URL="https://maven.pkg.github.com/${{ github.repository }}"
          echo "Deploying artifacts to GitHub Packages at $REPO_URL"
          mvn deploy -DskipTests -DaltDeploymentRepository=github::default::$REPO_URL

